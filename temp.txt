args = [
    type, 
    tranforms, 
    full_dataset,
    train_size
    val_size
    test_size
    batch_size
    num_workers
    name <-- name of the run
    project_name <-- folder on wandb
    criterion
    optimizer
    lr
    model
    epochs
    backbone
]

separate classifiers args = [
    disease_model
    severity_model
]

